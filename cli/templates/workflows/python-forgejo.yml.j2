# SuperDeploy Workflow - Python (Cara Framework)
# Auto-generated - DO NOT edit manually
# Regenerate: superdeploy generate -p {{ project }}

name: Deploy {{ app }}
on:
  push:
    branches: [main, production]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Deploy with SuperDeploy
        run: |
          curl -sSL https://raw.githubusercontent.com/{{ github_org }}/superdeploy/main/scripts/deploy.sh | bash
        env:
          PROJECT: {{ project }}
          APP: {{ app }}

