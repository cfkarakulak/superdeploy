# =============================================================================
# cheapa - Project Configuration
# =============================================================================
# Auto-generated by: superdeploy init -p cheapa
# Edit this file, then run: superdeploy generate -p cheapa
# =============================================================================

project: cheapa
description: cheapa project
created_at: 2025-10-28T17:40:41.250329

# =============================================================================
# Cloud Provider Configuration
# =============================================================================
cloud:
  # GCP Configuration
  gcp:
    project_id: "galvanic-camp-475519-d6"  # EDIT THIS
    region: "us-central1"
    zone: "us-central1-a"
  
  # SSH Configuration (for VM access)
  ssh:
    key_path: "~/.ssh/superdeploy_deploy"
    public_key_path: "~/.ssh/superdeploy_deploy.pub"
    user: "superdeploy"

# =============================================================================
# Docker Registry Configuration
# =============================================================================
docker:
  registry: "docker.io"  # Docker Hub or custom registry
  organization: "c100394"  # EDIT THIS - Docker organization
  username: "c100394"  # EDIT THIS
  # Note: Set DOCKER_TOKEN environment variable for authentication

# =============================================================================
# VMs (Infrastructure)
# =============================================================================
# Note: Orchestrator VM is managed globally in shared/orchestrator.yml
# Only define your project-specific VMs here
vms:
  web:
    count: 1
    machine_type: e2-small
    disk_size: 10
    services: []  # No infrastructure services, just apps
  
  api:
    count: 1
    machine_type: e2-small
    disk_size: 10
    services: []
  
  worker:
    count: 1
    machine_type: e2-medium
    disk_size: 20
    services:
      - postgres
      - rabbitmq
      - mongodb

# =============================================================================
# Addons Configuration
# =============================================================================
# Note: Forgejo is on orchestrator VM (shared/orchestrator.yml)
# Only define addons that run on YOUR project VMs
addons:
  rabbitmq:
    version: "3.12-management-alpine"
  postgres:
    version: "15-alpine"
  caddy:
    version: "2-alpine"

# =============================================================================
# Application Services
# =============================================================================
# Specify which VM each app runs on using 'vm' parameter
apps:
  dashboard:
    path: /Users/cfkarakulak/Desktop/cheapa.io/hero/app-repos/dashboard
    vm: web        # ← Dashboard runs on 'web' VM
    port: 3000
  
  api:
    path: /Users/cfkarakulak/Desktop/cheapa.io/hero/app-repos/api
    vm: api        # ← API runs on 'api' VM
    port: 8000
  
  services:
    path: /Users/cfkarakulak/Desktop/cheapa.io/hero/app-repos/services
    vm: worker     # ← Services run on 'worker' VM
    port: 8001

# =============================================================================
# GitHub Configuration
# =============================================================================
github:
  organization: "cheapaio"  # EDIT THIS - GitHub organization

# =============================================================================
# Network Configuration
# =============================================================================
network:
  docker_subnet: 172.31.0.0/24

# =============================================================================
# Monitoring (Optional)
# =============================================================================
monitoring:
  enabled: true
  prometheus: true
  grafana: true

# =============================================================================
# Domain (Optional)
# =============================================================================
domain: ""
