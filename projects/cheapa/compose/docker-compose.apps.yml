# App Services for cheapa
# Auto-generated by: superdeploy generate

networks:
  cheapa-network:
    name: cheapa-network
    external: true

services:

  api:
    image: ${DOCKER_REGISTRY:-docker.io}/${DOCKER_ORG}/api:${API_TAG}
    container_name: cheapa-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env.api
    networks:
      - cheapa-network

  dashboard:
    image: ${DOCKER_REGISTRY:-docker.io}/${DOCKER_ORG}/dashboard:${DASHBOARD_TAG}
    container_name: cheapa-dashboard
    restart: unless-stopped
    ports:
      - "8010:8010"
    env_file:
      - .env.dashboard
    networks:
      - cheapa-network

  services:
    image: ${DOCKER_REGISTRY:-docker.io}/${DOCKER_ORG}/services:${SERVICES_TAG}
    container_name: cheapa-services
    restart: unless-stopped
    ports:
      - "8020:8020"
    env_file:
      - .env.services
    networks:
      - cheapa-network