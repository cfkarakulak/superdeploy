# =============================================================================
# Cheapa Project - Runtime Secrets
# =============================================================================
# Copy this file to secrets.env and fill in your values
#
# USAGE:
#   cp secrets.env.example secrets.env
#   # Edit secrets.env with your values
#   superdeploy sync -p cheapa

# =============================================================================
# 🐘 PostgreSQL (Cheapa Database)
# =============================================================================
POSTGRES_HOST=postgres                  # Internal DNS name
POSTGRES_USER=cheapa_user
POSTGRES_PASSWORD=                      # ✏️ Generate strong password
POSTGRES_DB=cheapa_db
POSTGRES_PORT=5432

# =============================================================================
# 🐰 RabbitMQ (Cheapa Message Queue)
# =============================================================================
RABBITMQ_HOST=rabbitmq                  # Internal DNS name
RABBITMQ_USER=cheapa_user
RABBITMQ_PASSWORD=                      # ✏️ Generate strong password
RABBITMQ_PORT=5672

# =============================================================================
# 🔴 Redis (Cheapa Cache)
# =============================================================================
REDIS_HOST=redis                        # Internal DNS name
REDIS_PASSWORD=                         # ✏️ Generate strong password
REDIS_PORT=6379

# =============================================================================
# 🔑 API Secret Key
# =============================================================================
API_SECRET_KEY=                         # ✏️ Min 32 chars (openssl rand -hex 32)

# =============================================================================
# 🐛 Optional: Sentry (Error Tracking)
# =============================================================================
SENTRY_DSN=                             # Optional

# =============================================================================
# 📝 Notes:
# =============================================================================
# ✅ These secrets are synced to:
#    - Forgejo Actions (for deployment)
#    - GitHub Environments (for builds)
# ✅ Use 'superdeploy sync -p cheapa' to sync after changes
# ✅ Never commit secrets.env to git (it's in .gitignore)
