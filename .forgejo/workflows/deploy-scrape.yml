name: 🔍 Deploy SCRAPE VM

on:
  push:
    branches:
      - master
    paths:
      - 'deploy/compose/vm2-scrape/**'
      - '.forgejo/workflows/deploy-scrape.yml'
  workflow_dispatch:

env:
  SCRAPE_HOST: 10.0.0.7
  SCRAPE_USER: superdeploy
  COMPOSE_DIR: /opt/superdeploy/compose

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: 📦 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Generate .env from Forgejo Secrets
        run: |
          cat > /tmp/scrape.env << EOF
          # Generated by Forgejo Actions on $(date)
          
          # Core Services Connection
          CORE_API_HOST=${{ vars.CORE_INTERNAL_IP || '10.0.0.5' }}
          CORE_API_PORT=8000
          CORE_DB_HOST=${{ vars.CORE_INTERNAL_IP || '10.0.0.5' }}
          CORE_DB_PORT=5432
          CORE_DB_NAME=superdeploy
          CORE_DB_USER=superdeploy
          CORE_DB_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}
          
          # RabbitMQ Configuration
          RABBITMQ_HOST=${{ vars.CORE_INTERNAL_IP || '10.0.0.5' }}
          RABBITMQ_PORT=5672
          RABBITMQ_USER=superdeploy
          RABBITMQ_PASSWORD=${{ secrets.RABBITMQ_PASSWORD }}
          RABBITMQ_VHOST=/
          RABBITMQ_QUEUE_NAME=scraping_jobs
          
          # Proxy Configuration
          PROXY_REGISTRY_HOST=${{ vars.CORE_INTERNAL_IP || '10.0.0.5' }}
          PROXY_REGISTRY_PORT=8080
          PROXY_TIMEOUT=30
          PROXY_RETRY_COUNT=3
          PROXY_ROTATION_INTERVAL=300
          
          # Worker Configuration
          WORKER_CONCURRENCY=2
          WORKER_LOG_LEVEL=info
          WORKER_NAME=scrape-worker
          PLAYWRIGHT_TIMEOUT=30000
          PLAYWRIGHT_HEADLESS=true
          
          # Scraping Configuration
          USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36
          MAX_RETRIES=3
          DOWNLOAD_DELAY=1
          CONCURRENT_REQUESTS=5
          
          # Monitoring
          ENABLE_METRICS=true
          SENTRY_DSN=${{ secrets.SENTRY_DSN || '' }}
          EOF
          
          echo "✅ .env file generated from secrets"

      - name: 📤 Copy files to SCRAPE VM
        run: |
          # Copy .env file
          scp -o StrictHostKeyChecking=no /tmp/scrape.env ${{ env.SCRAPE_USER }}@${{ env.SCRAPE_HOST }}:${{ env.COMPOSE_DIR }}/.env
          
          # Copy compose files
          scp -o StrictHostKeyChecking=no -r deploy/compose/vm2-scrape/* ${{ env.SCRAPE_USER }}@${{ env.SCRAPE_HOST }}:${{ env.COMPOSE_DIR }}/
          
          echo "✅ Files copied to SCRAPE VM"

      - name: 🐳 Deploy on SCRAPE VM
        run: |
          ssh -o StrictHostKeyChecking=no ${{ env.SCRAPE_USER }}@${{ env.SCRAPE_HOST }} << 'ENDSSH'
            cd ${{ env.COMPOSE_DIR }}
            
            # Pull latest images
            docker compose pull 2>&1 || echo "⚠️  No pre-built images, will build locally"
            
            # Deploy services
            docker compose up -d --build
            
            echo "✅ Services deployed on SCRAPE VM"
          ENDSSH

      - name: ⏳ Wait and check health
        run: |
          echo "⏳ Waiting 10s for services..."
          sleep 10

      - name: 📊 Check service status
        run: |
          ssh -o StrictHostKeyChecking=no ${{ env.SCRAPE_USER }}@${{ env.SCRAPE_HOST }} << 'ENDSSH'
            cd ${{ env.COMPOSE_DIR }}
            
            echo "=== Container Status ==="
            docker compose ps
            
            echo ""
            echo "=== Worker Logs (last 20 lines) ==="
            docker compose logs --tail=20 worker || true
          ENDSSH

      - name: 🧹 Cleanup
        run: |
          rm -f /tmp/scrape.env
          ssh -o StrictHostKeyChecking=no ${{ env.SCRAPE_USER }}@${{ env.SCRAPE_HOST }} \
            "docker image prune -f"
          echo "✅ Cleanup complete"

      - name: 🎉 Deployment Summary
        run: |
          echo "╔══════════════════════════════════════════════════════════════╗"
          echo "║  ✅ SCRAPE VM Deployment Successful                          ║"
          echo "╚══════════════════════════════════════════════════════════════╝"
          echo ""
          echo "🔍 SCRAPE VM: ${{ env.SCRAPE_HOST }}"
          echo "📝 Commit: ${{ github.sha }}"
          echo "👤 Author: ${{ github.actor }}"
          echo "⏰ Time: $(date)"

