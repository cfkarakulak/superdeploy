name: 🚀 Deploy CORE VM

on:
  push:
    branches:
      - master
    paths:
      - 'deploy/compose/vm1-core/**'
      - '.forgejo/workflows/deploy-core.yml'
  workflow_dispatch:

env:
  COMPOSE_DIR: /opt/superdeploy/compose

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: 📦 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Generate .env from Forgejo Secrets
        run: |
          cat > /tmp/core.env << EOF
          # Generated by Forgejo Actions on $(date)
          
          # Database Configuration
          POSTGRES_DB=superdeploy
          POSTGRES_USER=superdeploy
          POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}
          POSTGRES_HOST=postgres
          POSTGRES_PORT=5432
          
          # RabbitMQ Configuration
          RABBITMQ_DEFAULT_USER=superdeploy
          RABBITMQ_DEFAULT_PASS=${{ secrets.RABBITMQ_PASSWORD }}
          RABBITMQ_DEFAULT_VHOST=/
          RABBITMQ_HOST=rabbitmq
          RABBITMQ_PORT=5672
          RABBITMQ_MANAGEMENT_PORT=15672
          
          # API Configuration
          API_HOST=0.0.0.0
          API_PORT=8000
          API_WORKERS=2
          API_LOG_LEVEL=info
          API_SECRET_KEY=${{ secrets.API_SECRET_KEY }}
          API_DEBUG=false
          
          # Proxy Registry Configuration
          PROXY_REGISTRY_HOST=0.0.0.0
          PROXY_REGISTRY_PORT=8080
          PROXY_REGISTRY_API_KEY=${{ secrets.PROXY_REGISTRY_API_KEY }}
          
          # Dashboard Configuration
          DASHBOARD_HOST=0.0.0.0
          DASHBOARD_PORT=3000
          DASHBOARD_API_URL=http://api:8000
          DASHBOARD_PROXY_REGISTRY_URL=http://proxy_registry:8080
          
          # Caddy Configuration
          CADDY_DOMAIN=${{ vars.CADDY_DOMAIN || '34.56.43.99' }}
          CADDY_EMAIL=${{ vars.CADDY_EMAIL || 'admin@superdeploy.local' }}
          
          # Network Configuration
          CORE_EXTERNAL_IP=${{ vars.CORE_EXTERNAL_IP || '34.56.43.99' }}
          CORE_INTERNAL_IP=${{ vars.CORE_INTERNAL_IP || '10.0.0.5' }}
          SCRAPE_INTERNAL_IP=${{ vars.SCRAPE_INTERNAL_IP || '10.0.0.7' }}
          PROXY_INTERNAL_IP=${{ vars.PROXY_INTERNAL_IP || '10.0.0.6' }}
          
          # Monitoring
          LOG_LEVEL=info
          ENABLE_METRICS=true
          SENTRY_DSN=${{ secrets.SENTRY_DSN || '' }}
          EOF
          
          echo "✅ .env file generated from secrets"

      - name: 📁 Copy files to deployment directory
        run: |
          sudo cp /tmp/core.env ${{ env.COMPOSE_DIR }}/.env
          sudo cp -r deploy/compose/vm1-core/* ${{ env.COMPOSE_DIR }}/
          sudo chown -R superdeploy:superdeploy ${{ env.COMPOSE_DIR }}
          echo "✅ Files copied to ${{ env.COMPOSE_DIR }}"

      - name: 🐳 Pull latest images
        run: |
          cd ${{ env.COMPOSE_DIR }}
          docker compose pull 2>&1 || echo "⚠️  No pre-built images, will build locally"

      - name: 🔨 Build and deploy services
        run: |
          cd ${{ env.COMPOSE_DIR }}
          docker compose up -d --build
          echo "✅ Services deployed"

      - name: ⏳ Wait for services to be healthy
        run: |
          echo "⏳ Waiting 30s for services..."
          sleep 30
          
          # Check API health
          for i in {1..10}; do
            if curl -f http://localhost:8000/health 2>/dev/null; then
              echo "✅ API is healthy"
              break
            fi
            echo "⏳ Waiting for API... ($i/10)"
            sleep 5
          done
          
          # Check Proxy Registry health
          for i in {1..10}; do
            if curl -f http://localhost:8080/health 2>/dev/null; then
              echo "✅ Proxy Registry is healthy"
              break
            fi
            echo "⏳ Waiting for Proxy Registry... ($i/10)"
            sleep 5
          done

      - name: 📊 Show deployment status
        run: |
          cd ${{ env.COMPOSE_DIR }}
          echo "=== Container Status ==="
          docker compose ps
          echo ""
          echo "=== Recent Logs ==="
          docker compose logs --tail=20

      - name: 🧹 Cleanup old images
        run: |
          docker image prune -f
          echo "✅ Cleanup complete"

      - name: 🎉 Deployment Summary
        run: |
          echo "╔══════════════════════════════════════════════════════════════╗"
          echo "║  ✅ CORE VM Deployment Successful                            ║"
          echo "╚══════════════════════════════════════════════════════════════╝"
          echo ""
          echo "🌐 Service URLs:"
          echo "  • Dashboard:      http://34.56.43.99/"
          echo "  • API:            http://34.56.43.99:8000"
          echo "  • API Health:     http://34.56.43.99:8000/health"
          echo "  • Proxy Registry: http://34.56.43.99:8080"
          echo "  • RabbitMQ UI:    http://34.56.43.99:15672"
          echo ""
          echo "📝 Commit: ${{ github.sha }}"
          echo "👤 Author: ${{ github.actor }}"
          echo "⏰ Time: $(date)"

