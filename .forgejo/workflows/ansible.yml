name: ⚙️ Ansible Configuration

on:
  push:
    branches:
      - main
    paths:
      - 'ansible/**'
  workflow_dispatch:
    inputs:
      target:
        description: 'Target VMs'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - core
          - scrape
          - proxy
          - forgejo

jobs:
  ansible:
    runs-on: self-hosted
    
    steps:
      - name: 📦 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Ansible
        run: |
          if ! command -v ansible &> /dev/null; then
            echo "📥 Installing Ansible..."
            sudo apt-get update -qq
            sudo apt-get install -y ansible
          fi
          ansible --version

      - name: 📝 Generate Ansible inventory
        run: |
          cd ansible
          
          # Use current IPs from Forgejo variables or defaults
          cat > inventories/dev.ini << EOF
          # Auto-generated by Forgejo Actions
          
          [core]
          vm-core-1 ansible_host=${{ vars.CORE_EXTERNAL_IP || '34.56.43.99' }} ansible_user=superdeploy
          
          [scrape]
          vm-scrape-1 ansible_host=${{ vars.SCRAPE_EXTERNAL_IP || '34.67.236.167' }} ansible_user=superdeploy
          
          [proxy]
          vm-proxy-1 ansible_host=${{ vars.PROXY_EXTERNAL_IP || '34.173.11.246' }} ansible_user=superdeploy
          
          [all:vars]
          ansible_python_interpreter=/usr/bin/python3
          ansible_become=yes
          ansible_become_method=sudo
          ansible_ssh_private_key_file={{ lookup('env', 'ANSIBLE_SSH_KEY') | default('~/.ssh/id_rsa', true) }}
          EOF
          
          echo "✅ Inventory generated"

      - name: 🚀 Run Ansible playbook
        run: |
          cd ansible
          
          TARGET="${{ github.event.inputs.target || 'all' }}"
          
          echo "🎯 Deploying to: $TARGET"
          
          if [ "$TARGET" = "all" ]; then
            ansible-playbook -i inventories/dev.ini playbooks/site.yml
          else
            ansible-playbook -i inventories/dev.ini playbooks/site.yml --tags "$TARGET"
          fi

      - name: ✅ Verify deployment
        run: |
          cd ansible
          
          echo "╔══════════════════════════════════════════════════════════════╗"
          echo "║  📊 Deployment Verification                                  ║"
          echo "╚══════════════════════════════════════════════════════════════╝"
          echo ""
          
          echo "=== CORE VM ==="
          ansible core -i inventories/dev.ini -m shell -a "docker compose -f /opt/superdeploy/compose/docker-compose.yml ps" || true
          
          echo ""
          echo "=== SCRAPE VM ==="
          ansible scrape -i inventories/dev.ini -m shell -a "docker compose -f /opt/superdeploy/compose/docker-compose.yml ps" || true
          
          echo ""
          echo "=== PROXY VM ==="
          ansible proxy -i inventories/dev.ini -m shell -a "docker compose -f /opt/superdeploy/compose/docker-compose.yml ps" || true

      - name: 🎉 Configuration Summary
        run: |
          echo ""
          echo "✅ Ansible configuration completed!"
          echo "📝 Commit: ${{ github.sha }}"
          echo "👤 Author: ${{ github.actor }}"
          echo "⏰ Time: $(date)"

