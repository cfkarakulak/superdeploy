name: 🌐 Deploy PROXY VM

on:
  push:
    branches:
      - main
    paths:
      - 'deploy/compose/vm3-proxy/**'
      - '.forgejo/workflows/deploy-proxy.yml'
  workflow_dispatch:

env:
  PROXY_HOST: 10.0.0.6
  PROXY_USER: superdeploy
  COMPOSE_DIR: /opt/superdeploy/compose

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: 📦 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Generate .env from Forgejo Secrets
        run: |
          cat > /tmp/proxy.env << EOF
          # Generated by Forgejo Actions on $(date)
          
          # Proxy Configuration
          PROXY_USER=proxyuser
          PROXY_PASSWORD=${{ secrets.PROXY_PASSWORD || 'change_me_in_production' }}
          PROXY_EXTERNAL_IP=${{ vars.PROXY_EXTERNAL_IP || '34.173.11.246' }}
          SOCKS5_PORT=1080
          HTTP_PROXY_PORT=3128
          
          # Proxy Registry Connection
          PROXY_REGISTRY_HOST=${{ vars.CORE_INTERNAL_IP || '10.0.0.5' }}
          PROXY_REGISTRY_PORT=8080
          PROXY_REGISTRY_API_KEY=${{ secrets.PROXY_REGISTRY_API_KEY }}
          
          # IP Monitoring Configuration
          IP_CHECK_INTERVAL=300
          IP_CHECK_URL=https://api.ipify.org
          IP_UPDATE_ENDPOINT=http://${{ vars.CORE_INTERNAL_IP || '10.0.0.5' }}:8080/api/proxies/update
          
          # Access Control
          ALLOWED_IPS=${{ vars.PROXY_ALLOWED_IPS || '' }}
          EOF
          
          echo "✅ .env file generated from secrets"

      - name: 📤 Copy files to PROXY VM
        run: |
          # Copy .env file
          scp -o StrictHostKeyChecking=no /tmp/proxy.env ${{ env.PROXY_USER }}@${{ env.PROXY_HOST }}:${{ env.COMPOSE_DIR }}/.env
          
          # Copy compose files
          scp -o StrictHostKeyChecking=no -r deploy/compose/vm3-proxy/* ${{ env.PROXY_USER }}@${{ env.PROXY_HOST }}:${{ env.COMPOSE_DIR }}/
          
          echo "✅ Files copied to PROXY VM"

      - name: 🐳 Deploy on PROXY VM
        run: |
          ssh -o StrictHostKeyChecking=no ${{ env.PROXY_USER }}@${{ env.PROXY_HOST }} << 'ENDSSH'
            cd ${{ env.COMPOSE_DIR }}
            
            # Pull latest images
            docker compose pull 2>&1 || echo "⚠️  No pre-built images, will build locally"
            
            # Deploy services
            docker compose up -d --build
            
            echo "✅ Services deployed on PROXY VM"
          ENDSSH

      - name: ⏳ Wait and test proxies
        run: |
          echo "⏳ Waiting 10s for services..."
          sleep 10
          
          echo ""
          echo "🧪 Testing HTTP Proxy..."
          curl -x http://${{ vars.PROXY_EXTERNAL_IP || '34.173.11.246' }}:3128 http://httpbin.org/ip || echo "⚠️  Proxy test failed"
          
          echo ""
          echo "🧪 Testing SOCKS5 Proxy..."
          curl --socks5 ${{ vars.PROXY_EXTERNAL_IP || '34.173.11.246' }}:1080 http://httpbin.org/ip || echo "⚠️  Proxy test failed"

      - name: 📊 Check service status
        run: |
          ssh -o StrictHostKeyChecking=no ${{ env.PROXY_USER }}@${{ env.PROXY_HOST }} << 'ENDSSH'
            cd ${{ env.COMPOSE_DIR }}
            
            echo "=== Container Status ==="
            docker compose ps
            
            echo ""
            echo "=== Service Logs (last 15 lines) ==="
            docker compose logs --tail=15
          ENDSSH

      - name: 🧹 Cleanup
        run: |
          rm -f /tmp/proxy.env
          ssh -o StrictHostKeyChecking=no ${{ env.PROXY_USER }}@${{ env.PROXY_HOST }} \
            "docker image prune -f"
          echo "✅ Cleanup complete"

      - name: 🎉 Deployment Summary
        run: |
          echo "╔══════════════════════════════════════════════════════════════╗"
          echo "║  ✅ PROXY VM Deployment Successful                           ║"
          echo "╚══════════════════════════════════════════════════════════════╝"
          echo ""
          echo "🌐 Proxy Services:"
          echo "  • HTTP Proxy:  http://${{ vars.PROXY_EXTERNAL_IP || '34.173.11.246' }}:3128"
          echo "  • SOCKS5:      socks5://${{ vars.PROXY_EXTERNAL_IP || '34.173.11.246' }}:1080"
          echo ""
          echo "📝 Commit: ${{ github.sha }}"
          echo "👤 Author: ${{ github.actor }}"
          echo "⏰ Time: $(date)"

